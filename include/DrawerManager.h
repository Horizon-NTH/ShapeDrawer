#pragma once

#include "Include.h"
/*
class DrawerManager
{
public:
	DrawerManager(const std::shared_ptr<hgui::kernel::Window>& window);

	void draw();

private:
	std::shared_ptr<hgui::kernel::Canvas> m_canvas;
	std::shared_ptr<hgui::kernel::Window> m_window;

	void set_main_menu();
};*/
